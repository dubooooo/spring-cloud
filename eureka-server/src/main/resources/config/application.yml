spring:
  application:
    name: eureka-server
server:
  port: 10000
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10000/eureka
    register-with-eureka: false
    fetch-registry: false
